services:
  postgres:
    image: postgres:16-alpine
    container_name: gestpro-crm-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: gestpro_admin
      POSTGRES_PASSWORD: gestpro_secret_2026
      POSTGRES_DB: gestpro_crm
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: gestpro-crm-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  pgdata:
